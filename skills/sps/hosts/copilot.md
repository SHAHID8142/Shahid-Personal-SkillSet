# GitHub Copilot host adapter

- Prefer project `AGENTS.md` + `./.sps/` memory
- Use Agent Skills paths when Copilot supports them
- Keep `/sps` lock in `.github/copilot-instructions.md` only as a short pointer if needed
- Specialist Claude plugins are not assumed
