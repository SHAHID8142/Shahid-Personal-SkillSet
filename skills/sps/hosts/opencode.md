# OpenCode / Cline / Roo / Kiro / Amp adapter notes

These hosts vary. Portable contract:

1. SKILL.md format when skills dirs exist
2. `AGENTS.md` + `./.sps/` always
3. Capability-aware fallbacks from CAPABILITY-MATRIX.md
4. Skill router install-or-instruct protocol still applies
