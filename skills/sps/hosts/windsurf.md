# Windsurf host adapter

- Install/mirror `/sps` into Windsurf-supported skills/rules locations via installer sync paths when available
- Keep rules concise; Windsurf may load broad rule files — prefer pointing to `./.sps/`
- Follow METHOD-CARD; no second orchestrator
